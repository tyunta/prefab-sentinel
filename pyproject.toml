[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[project]
name = "unitytool"
version = "0.1.52"
description = "Phase 1 scaffold for UnityTool CLI and MCP contracts."
requires-python = ">=3.11"
readme = "README.md"
dependencies = []

[project.scripts]
unitytool = "unitytool.cli:main"
